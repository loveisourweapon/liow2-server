#client:
#  build: client/
#  links:
#    - server
#  ports:
#    - "80:80"
#  volumes:
#    - client:/usr/src/myapp

server:
  build: .
  links:
    - mongo
  ports:
    - "80:3000"
  volumes:
    - .:/usr/src/app

mongo:
  image: mongo
#  ports:
#    - "27017:27017"
